@model CompaniesQueryModel

@{
    ViewData["Title"] = "Companies";
}
<form method="get">
    <div class="row mb-2">
        <div class="form-group col-md-6">
            <label asp-for="City"></label>
            <select asp-for="City" class="form-control form-inline">
                <option value="">All</option>
                @foreach (var city in Model.Cities)
                {
                    <option value="@city">@city</option>
                }
            </select>
        </div>
        <div class="form-group col-md-6">
            <label asp-for="Industry"></label>
            <select asp-for="Industry" class="form-control form-inline">
                <option value="">All</option>
                @foreach (var industry in Model.Industries)
                {
                    <option value="@industry.Value">@industry.Value</option>
                }
            </select>
        </div>
        <div class="form-group col-12">
            <label asp-for="SearchTerm"></label>
            <input asp-for="SearchTerm" class="form-control" type="text" placeholder="Search" aria-label="Search">
        </div>
    </div>
    <div class="d-flex justify-content-center mb-4">
        <button class="btn btn-outline-primary btn-rounded btn-sm" type="submit">Search</button>
    </div>
</form>

<hr />

<div class="row">
    @foreach (var company in Model.Companies)
    {
        <div class="col-md-4 col-lg-3">
            <div class="card bg-light mb-3" style="max-width: 16rem;">
                <div class="card-header">
                    <div>
                        <a asp-action="Details" asp-controller="Companies" asp-route-id="@company.Id">
                            @if (company.LogoUrl != null)
                            {
                                <img class="card-img-top" src="@company.LogoUrl" alt="@company.Name - Logo">
                            }
                            else
                            {
                                <img class="card-img-top" src="~/default pictures/Company Logo.jpg" alt="@company.Name - Logo">
                            }
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <h5 class="card-title text-center">@company.Name</h5>
                    <p class="card-text">Industry: @company.IndustryName</p>
                    <p class="card-text">Category: @company.CategoryName</p>
                    <p class="card-text">Location: @company.AddressCity, @company.AddressCountry</p>
                    <p class="card-text">Founded: @company.Founded</p>
                    <a asp-action="Details" asp-controller="Companies" asp-route-id="@company.Id" class="btn btn-outline-primary">Details</a>
                </div>
            </div>
        </div>
    }
</div>

<hr />